# Actual makefile is in GNUmakefile

all:
	@echo "This project's makefile is only compatible with GNU Make."
	@echo ""
	@echo "If you used 'make', try 'gmake' or 'gnumake' instead."
	@echo "If you don't have GNU Make, go install it."
